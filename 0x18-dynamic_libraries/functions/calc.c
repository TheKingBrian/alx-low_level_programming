#include <stdio.h>
/**
 * add - adds two numbers
 * @a: the 1st number to be added
 * @b: the 2nd number to be added
 * Return: plus between two numbers.
 */
int add(int a, int b)
{
return (a + b);
}
/**
 * sub - minus between 2 values
 * @a: the 1st number to be added
 * @b: the 2nd number to be added
 * Return: sebarates all the 2 values
 */
int sub(int a, int b)
{
return (a - b);
}
/**
 * mul - times 2 values
 * @a: the 1st number to be added
 * @b: the 2nd number to be added
 * Return: times the 2 values
 */
int mul(int a, int b)
{
return (a * b);
}
/**
 * mul - div ea the values
 * @a: the 1st number to be added
 * @b: the 2nd number to be added
 * Return: div 2 int values
 */
int div(int a, int b)
{
return (a / b);
}
/**
 * mul - mod the vales
 * @a: the 1st number to be added
 * @b: the 2nd number to be added
 * Return:mod pf 2 sums.
 */
int mod(int a, int b)
{
return (a % b);
}
