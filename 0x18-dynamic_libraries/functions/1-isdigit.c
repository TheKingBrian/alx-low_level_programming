#include "main.h"
/**
 * _isdigit - it runs between 0 and 9
 * @c: character
 * Return:when its 1that means it works
 */
int _isdigit(int c)
{
if (c >= '0' && c <= '9')
return (1);
else
return (0);
}
